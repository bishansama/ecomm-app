apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: ecomm-app
  labels:
    app: notification-service
    service: notification-service
spec:
  type: ClusterIP
  ports:
  - port: 8890
    targetPort: 8890
    protocol: TCP
    name: http
  selector:
    app: notification-service