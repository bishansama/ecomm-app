apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: ecomm-app
  labels:
    app: payment-service
    service: payment-service
spec:
  type: ClusterIP
  ports:
  - port: 8891
    targetPort: 8891
    protocol: TCP
    name: http
  selector:
    app: payment-service