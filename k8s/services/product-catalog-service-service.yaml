apiVersion: v1
kind: Service
metadata:
  name: product-catalog-service
  namespace: ecomm-app
  labels:
    app: product-catalog-service
    service: product-catalog-service
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: product-catalog-service