server:
  port: 8888
  servlet:
    context-path: /

spring:
  application:
    name: order-service

# OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  packages-to-scan: com.ecommapp.orderservice.controller