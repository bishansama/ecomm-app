server:
  port: 8891
  servlet:
    context-path: /

spring:
  application:
    name: payment-service

# OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  packages-to-scan: com.ecommapp.paymentservice.controller